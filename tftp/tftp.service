[Unit]
Description=Tftp Server
Requires=tftp.socket
Documentation=man:in.tftpd

[Service]
ExecStart=/usr/sbin/in.tftpd -c -s -u tftp -U 111 -a '0.0.0.0' -vvvv /var/lib/tftpboot
StandardInput=socket

[Install]
Also=tftp.socket

