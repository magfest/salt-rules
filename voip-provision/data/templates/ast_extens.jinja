{% for extension, vals in extens.items() %}
{% if not vals.get('special', False) and 'users' in vals and vals['users'] %}
exten => {{ extension }},1,Dial({{ '&'.join(vals['users']) }},60)
same => n,Hangup
{% for alt in vals.get('alt_extens', []) %}
exten => {{ alt }},1,Dial({{ '&'.join(vals['users']) }},60)
same => n,Hangup
{% endfor %}
{% endif %}
{% endfor %}
